# i3blocks config file
#
# Please see man i3blocks for a complete reference!
# The man page is also hosted at http://vivien.github.io/i3blocks
#
# List of valid properties:
#
# align
# color
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent

# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
command=/usr/lib/i3blocks/$BLOCK_NAME
separator_block_width=30
markup=none
separator=false

[wifi]
command=~/.config/i3blocks/blocklets/wifi
instance=wlan0
#label=
label=
interval=30

[iface]
command=~/.config/i3blocks/blocklets/iface
label=
instance=enp0s31f6
interval=30

[iface]
command=~/.config/i3blocks/blocklets/iface
label=
instance=ens9
interval=30

[iface]
command=~/.config/i3blocks/blocklets/iface
label=
instance=enp0s20u1u1u3
interval=30

[openconnect]
label=
command=~/.config/i3blocks/blocklets/openconnect.sh
interval=30
signal=11

[volume]
label=
instance=Master
interval=once
signal=10

[battery]
label=
command=~/.config/i3blocks/blocklets/battery.py
interval=60

[keyboard]
label=
command=~/.config/i3blocks/blocklets/keyboard.sh
interval=once
signal=12

[time]
#label=
command=date '+%b %d %H:%M'
interval=60
separator=true
