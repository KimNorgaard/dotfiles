# i3blocks config file
#
# Please see man i3blocks for a complete reference!
# The man page is also hosted at http://vivien.github.io/i3blocks
#
# List of valid properties:
#
# align
# color
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent

# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
command=/usr/lib/i3blocks/$BLOCK_NAME
separator_block_width=30
markup=none
separator=true

# [wifi]
# command=~/.config/i3blocks/blocklets/go/wifi
# instance=wlan0
# interval=30
# markup=pango
#
# [iface]
# command=~/.config/i3blocks/blocklets/go/iface
# instance=enp0s31f6
# interval=30
#
# [iface]
# command=~/.config/i3blocks/blocklets/go/iface
# instance=ens9
# interval=30
#
# [iface]
# command=~/.config/i3blocks/blocklets/go/iface
# instance=enp0s20u1u1u3
# interval=30

# Moved to notificationss
# [openconnect]
# command=~/.config/i3blocks/blocklets/go/openconnect
# interval=30
# signal=11

# Moved to notificationss
# [volume]
# command=~/.config/i3blocks/blocklets/go/volume
# interval=once
# signal=10

#[battery]
#command=~/.config/i3blocks/blocklets/go/battery
#interval=60

# Moved to notificationss
#[keyboard]
#command=~/.config/i3blocks/blocklets/keyboard.sh
#interval=once
#signal=12

[time]
command=date '+%b %d %H:%M'
interval=60
separator=true
